Go Sample
---------

This sample demonstrates suppressing an unreachable CVE via reachability. It is a placeholder; replace with a minimal Go module exposing an entry point in `cmd/server` and a vulnerable transitive dependency that is not invoked.


